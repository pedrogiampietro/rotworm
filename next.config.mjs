/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'placehold.co', 'github.com', 'cdn.discordapp.com']
    }
};

export default nextConfig;
